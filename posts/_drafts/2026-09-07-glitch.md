---
title: glitch - audio destruction, in the browser and in your daw
date: 2026-09-07
dek: TODO — one line. draw where it breaks; every pass mutates.
image: assets/glitch-screen.png
image_alt: glitch running in the browser — six stage lanes drawn with blocks over a waveform, playhead mid-pass
---

TODO — opening. where this came from: the sequencer's tape/data-bender, the "just hope it works" glitch stage, wanting a version you could add to a track on purpose.

TODO — what it is. six stages in a fixed order (crush, noise, drive, chop, glitch, feedback). you draw blocks into lanes to say where each one hits; every block carries its own settings. every pass mutates what you drew. loop a region, lock the grid to the file's bpm, reseed for a random set. print the last pass or the whole arc.

[try glitch](../glitch.html)

TODO — the plugin. same idea as a native au/vst3 for logic/ableton/etc — lanes run on the host's bars instead of a file. free, signed, apple silicon + intel.

<!-- TODO: plugin download link once plugins.html is public. current unlisted url: ../plugins-886ad9.html#glitch -->
[download the glitch plugin](../plugins-886ad9.html#glitch)

TODO — closing / what's next.

<!-- Chris: page copy from glitch.html sub — "audio destruction — drop a wav, get a worse wav. draw where each stage hits. every pass mutates what you drew."
     plugins page 04 copy (yours) — "audio destruction masquerading as a step sequencer. draw into the lanes, adjust parameters, ruin whatever you've got going. pretty simple - use it as a loop or crank the mutate knob for random shit every repeat. lots of toys to play with here, but also just spam "reseed" and it'll give you a random set every time." -->
