.DS_Store
.claude/
node_modules/
dist/
_site/

# vst/ build artifacts (Faust + JUCE)
vst/*/Builds/
vst/*/build/
vst/*/cmake-build-*/
vst/*/*.vst3
vst/*/*.component
# Regenerated by faust2juce + Projucer on every build — kept out of git.
# combined.jucer, Source/, assets/ ARE tracked (hand-edited / hand-authored).
vst/*/juce/combined/Builds/
vst/*/juce/combined/JuceLibraryCode/
vst/*/juce/combined/FaustPluginProcessor.cpp
# faust2au build temp dir (legacy build.sh)
vst/*/dsp/faustAUtmp.*/
